apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: vidalcasino
spec:
  type: ClusterIP
  selector:
    app: frontend-service
  ports:
    - port: 8080
      targetPort: 8080
